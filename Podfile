# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'CardPartsSample' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'CardParts'

  # Pods for CardPartsSample

  target 'CardPartsSampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CardPartsSampleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
